import styled from "styled-components";

export const SectionContainer = styled.section `
    padding: 3rem;
`

export const HeaderCalender = styled.header `
    margin-bottom: 5rem;
    
    p {
        color: #07232E;
        font-size: 1.4rem;
        font-weight: 600;
    }
`

export const HeaderProfile = styled.div `
    display: flex;
    gap: 1.7rem;
    align-items: center;
    margin-top: 2.05rem;

    .name {
        color: #07232E;
        font-size: 1.6rem;
    }

    .company {
        color: #909CAA;
        text-transform: uppercase;
        font-size: 1.2rem;
        font-weight: 400;
        letter-spacing: .06rem;
        margin-top: .4rem;
    }
`


